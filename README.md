# arquitetura-limpa

Projeto do curso Arquitetura Limpa do FormacaoDEV

1. npm init -y
2. npm i -D typescript
3. npx tsc --init
4. criar .gitignore
5. criar pastas ./src e ./test
6. npm i -D jest ts-jest @types/jest @types/node
7. npm i -D ts-node-dev
8. npx ts-jest config:init
